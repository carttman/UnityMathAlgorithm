using UnityEngine;

public class SecondBullet : MonoBehaviour
{
    float speed = 10f;
    void Start()
    {
        
    }

    void Update()
    {
        //transform.position += transform.right * (speed * Time.deltaTime);
    }
}
